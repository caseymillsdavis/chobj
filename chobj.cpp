#include "chobj.h"

static void _get_num_elements(const std::vector<std::vector<unsigned> > &objects)
{
}

void obj_free(const std::vector<std::vector<unsigned> > &objects,
              std::vector<std::pair<std::vector<unsigned>, std::vector<unsigned> > > &obj_free_tuples)
{
}
